description: "maps: offline RGB565 slippy-map viewer (LCD launcher program + browser MapWindow). Reads GPS fix via ephemeral storage."
url: https://github.com/reticulous/maps
version: "0.1.0"
license: "Apache-2.0"
targets:
  - esp32s3
dependencies:
  idf:
    version: ">=5.5"
  espressif/esp_jpeg:
    version: "^1.1"
